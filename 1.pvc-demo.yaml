piVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: demo-1
spec:
    accessModes:
    - ReadWriteOnce
    resources:
        requests:
            storage: 10Gi
    storageClassName: nutanix-volume
